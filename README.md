# Docker Lab Exercises
This is my repository for Docker practice.

## Project Structure
- **database-exercise/mysql**: Dockerfile for custom MySQL image.
- **database-exercise/postgres**: Dockerfile for custom PostgreSQL image.
####################################################
## Exercise 2: Multi-Database with Docker Compose
In this exercise, I managed two databases (MySQL & PostgreSQL) using a single `docker-compose.yml` file.

### How to Run:
1. Navigate to the project directory.
2. Run the command:
```bash
   docker compose up -d
   

